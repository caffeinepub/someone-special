{
  "frontend/src/index.css": "Global styles with romantic rose/blush OKLCH color palette, Google Fonts import for Playfair Display and Lato, and CSS keyframe animations for floating petals, heart pulse/float effects, and note card entrance animations.",
  "frontend/tailwind.config.js": "Extended Tailwind config with romantic serif/sans font families, rose/cream/blush color tokens, custom border radii, rose-tinted box shadows, and animation utilities.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations including a new `useUpdateHeartPosition` mutation that calls `actor.updatePosition` and invalidates the heartNotes cache on success.",
  "frontend/src/components/FloatingPetals.tsx": "Ambient background component that renders 18 randomly positioned SVG heart/petal shapes with CSS float animations for a romantic atmosphere.",
  "frontend/src/components/NoteCard.tsx": "Popover card showing a heart note's message, creator, and date with edit/delete actions; accepts `translateX`, `openUpward`, `openLeftward`, and `heartSize` props to position itself fully within the viewport from any direction, with the arrow pointer always pointing back toward the heart.",
  "frontend/src/components/HeartCanvas.tsx": "Interactive canvas rendering draggable heart icons; supports click-to-open NoteCard and mouse/touch drag-to-reposition with optimistic position updates, backend persistence via `useUpdateHeartPosition`, and smart card layout logic unchanged from Version 6.",
  "frontend/src/components/AddHeartForm.tsx": "Collapsible form for adding heart notes with message/creator fields, inline validation (prevents empty submission), spinner during pending state, error display, and success animation that only triggers after a confirmed backend response.",
  "frontend/src/components/EditNoteModal.tsx": "Full-screen modal overlay for editing an existing heart note's message, with loading state and cancel/save actions.",
  "frontend/src/App.tsx": "Main application component that renders the full romantic layout including header, hero banner, personal greeting, heart canvas, add-heart form, admin greeting editor, and footer. Removed the now-unused `onEdit`/`onDelete` props, `editingNote` state, `useDeleteHeartNote` import, and top-level `EditNoteModal` since edit/delete are handled internally by `NoteCard`.",
  "frontend/src/components/PersonalGreeting.tsx": "Displays the personal greeting message fetched from the backend in a styled serif italic quote format with decorative heart dividers; shows a default message if none is set and a subtle loading indicator while fetching.",
  "frontend/src/components/AdminGreetingEditor.tsx": "Collapsible admin editor component for updating the site's personal greeting message, with a pre-filled textarea, character counter, loading/success states, and styling consistent with the romantic theme."
}